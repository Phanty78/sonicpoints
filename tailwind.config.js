module.exports = {
  theme: {
    extend: {
      animation: {
        shake: 'var(--animation-shake)',
      },
      backgroundImage: {
        sonic: 'var(--sonic-gradient)',
        ring: 'var(--ring-gradient)',
        gemx: 'var(--gemx-gradient)',
      },
    },
  },
};
